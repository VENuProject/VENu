#pragma strict


var dot = gameObject;

function Start () {

}
/*
public boolean Blue(){
	if(Blue == true && Red == false)
	{
		dot.gameObject = Color.blue;
		Debug.Log("The dot is red")

	}
	else if(Red == true && Blue == false)
	{
		dot.gameObject = Color.red;
		Debug.Log("The dot is red");
	}
	else
	{
		Debug.Log("The dot's color remains unchanged");
	}
}

public boolean Red(){
	if(Blue == true && Red == false)
	{
		dot.gameObject = Color.blue;
		Debug.Log("The dot is blue")

	}
	else if(Red == true && Blue == false)
	{
		dot.gameObject = Color.red;
		Debug.Log("The dot is red");
	}
	else
	{
		Debug.Log("The dot's color remains unchanged");
	}
	*/
function Update () {

}